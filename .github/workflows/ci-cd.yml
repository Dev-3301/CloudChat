name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
    paths:
      - 'auth/**'
      - 'chat/**'
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        services: [auth,chat]
    steps:
      - uses: actions/checkout@v2
      - name: Build and push Docker image

